{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <div class="container">
        <div class="text-center mt-4">
            <h1>Auction List</h1>
        </div>
        <div class="row mt-5 mb-4 mt-4">
            {% for auction in auctions %}
                <div class="col-md-6 col-lg-3 mb-3">
                    <div class="card" style="width: 18rem;">
                        <img src="{{ asset('uploads/' ~ auction.image) }}" class="card-img-top" alt="{{ auction.title }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ auction.title }}</h5>
                            <div class="card-text">
                                <p>{{ (auction.price/100)| format_currency('EUR',locale="fr") }}</p>
                                <p>Du {{ auction.dateOpen|format_datetime(locale='fr') }} au {{ auction.dateClose|format_datetime(locale='fr') }}</p>
                                <button type="button" class="btn btn-secondary">Enchèrir</button>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
